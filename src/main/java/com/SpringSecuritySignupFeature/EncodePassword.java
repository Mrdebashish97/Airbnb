package com.SpringSecuritySignupFeature;

public class EncodePassword {
    // In order to encode password in spring security, we have one class here
    // called as BCryptPasswordEncoder(). // this class has a method called as encode().
    // Put it in reference variable , PasswordEncoder e = new BCryptPasswordEncoder();
    // so go to AuthController

}
